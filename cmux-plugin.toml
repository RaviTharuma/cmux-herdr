[plugin]
name = "cmux-herdr"
kind = "sidebar"
version = "0.7.0"
description = "Herdr in native cmux chrome: CLI, watch, status pills, pane mirroring"

[run]
command = ["bin/cmux-herdr-sidebar"]

[build]
command = ["bin/cmux-herdr-fetch"]
