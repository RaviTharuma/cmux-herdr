[plugin]
name = "cmux-herdr"
kind = "sidebar"
version = "0.6.0"
description = "Herdr in native cmux chrome: live workspaces, Reorderable, pane mirroring"

[run]
command = ["bin/cmux-herdr-sidebar"]

[build]
command = ["chmod", "+x", "bin/cmux-herdr-sidebar"]
